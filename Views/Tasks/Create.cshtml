@model Eisenhower.Models.ViewModels.TaskViewModel
@{
    ViewData["Title"] = "Create Task";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card shadow-sm">
            <div class="card-header bg-success text-white">
                <h4 class="mb-0"><i class="bi bi-plus-circle me-2"></i>Create New Task</h4>
            </div>
            <div class="card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                    
                    <div class="mb-3">
                        <label asp-for="Title" class="form-label">Task Title <span class="text-danger">*</span></label>
                        <input asp-for="Title" class="form-control" placeholder="Enter task title" autofocus />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>
                    
                    <div class="mb-3">
                        <label asp-for="Notes" class="form-label">Notes</label>
                        <textarea asp-for="Notes" class="form-control" rows="3" placeholder="Add any notes or details..."></textarea>
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>
                    
                    <div class="mb-3">
                        <label asp-for="Quadrant" class="form-label">Quadrant <span class="text-danger">*</span></label>
                        <select asp-for="Quadrant" class="form-select">
                            <option value="0">Q1: Urgent & Important - Do First</option>
                            <option value="1">Q2: Not Urgent but Important - Schedule</option>
                            <option value="2">Q3: Urgent but Not Important - Delegate</option>
                            <option value="3">Q4: Not Urgent & Not Important - Eliminate</option>
                        </select>
                        <span asp-validation-for="Quadrant" class="text-danger"></span>
                        <div class="form-text">
                            <strong>Q1:</strong> Crisis, deadlines, problems<br />
                            <strong>Q2:</strong> Planning, prevention, improvement<br />
                            <strong>Q3:</strong> Interruptions, some meetings<br />
                            <strong>Q4:</strong> Time wasters, pleasant activities
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <label asp-for="DueDate" class="form-label">Due Date</label>
                        <input asp-for="DueDate" type="date" class="form-control" />
                        <span asp-validation-for="DueDate" class="text-danger"></span>
                    </div>
                    
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success">
                            <i class="bi bi-check-lg me-1"></i>Create Task
                        </button>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-x-lg me-1"></i>Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
